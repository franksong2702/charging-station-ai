# 兜底流程优化方案 - 分层客服架构

> **版本**: v2.0  
> **创建时间**: 2026-04-09  
> **上次更新**: 2026-04-09  
> **状态**: P1/P2 已修复 ✅，等待分层客服实现  
> **目标**: 解决"无条件兜底"问题，实现分层客服

---

## 一、问题背景

### 当前问题（"无条件兜底"）

markdown
用户："我要退款"
↓
AI："哦，那我明白了，您的问题大概是这样的：退款。
请您留下手机号和车牌号..."


**问题**：
1. ❌ 没有问"为什么退款"
2. ❌ 没有试图解决问题
3. ❌ 直接收集信息 → 创建工单
4. ❌ 问题总结太简略（如"用户要求退款"）

### 理想状态（"分层客服"）

用户："我要退款"
↓
AI："好的，请问退款的原因是什么呢？
是扣费错误、充电失败、还是其他原因？"
↓
用户："充完电优惠券没用上，多扣了我钱"
↓
AI："明白了。根据您的情况，建议先在 App 里查看优惠券使用记录...
您看这样可以吗？"
↓
【用户接受】→ 问题解决，不创建工单
【用户不接受】→ 进入兜底流程


---

## 二、已完成的修复（v1.1）

### P1 修复：兜底流程中强制保持状态 ✅

**问题**：用户在兜底流程中，AI 错误地返回知识库答案而不是继续收集信息。

**修复**：`src/graphs/nodes/intent_recognition_node.py`

```python
# 【优化后的兜底退出判断 - 关键修复 P1】
if state.fallback_phase:
    if intent == "exit_fallback":
        logger.info("意图识别 - LLM 判断退出兜底")
    else:
        logger.info(f"意图识别 - 在兜底流程中，强制保持为 fallback，原意图：{intent}")
        intent = "fallback"  # 强制保持兜底
验证：已提交到 dev 分支，commit xxx

P2 修复：更严格的取消判断 ✅
问题：用户说"算了"被误判为取消兜底，但实际可能只是情绪宣泄。

修复：src/graphs/nodes/fallback_node.py

def _is_cancel(user_message: str) -> bool:
    """判断用户是否要取消 - 更严格的判断（修复 P2）"""
    cancel_keywords = ["取消", "不要了", "不用处理", "不需要", "不聊了", "再见"]
    # 移除了"算了"，避免误判情绪性表达
    msg = user_message.lower().strip()
    for kw in cancel_keywords:
        if msg == kw or msg.startswith(kw + ",") or msg.startswith(kw + "。"):
            return True
    return False
python
验证：已提交到 dev 分支，commit xxx

真实测试结果（修复前基准）
场景 ID	场景名称	结果	轮数	失败原因
S001	优惠券投诉	✅	4 轮	-
S002	充电故障	✅	4 轮	-
S003	退款投诉	❌	2 轮	无条件兜底
S004	模糊投诉	✅	5 轮	-
S005	情绪激动	❌	2 轮	误判为取消
S006	中途取消	✅	2 轮	-
S007	用户纠正	❌	3 轮	总结不准确
S008	分次提供	❌	4 轮	信息不更新
S009	语音格式	❌	3 轮	格式解析失败
S010	补充信息	❌	3 轮	信息不更新
通过率: 5/10 = 50%

预期修复后: P1/P2 修复后，S005 应该通过 → 6/10 = 60%

三、核心架构
分层客服架构
┌─────────────────────────────────────────────────────────────┐
│              分层客服架构                                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Level 1: 知识问答层（能直接解决的直接解决）                  │
│           ↓（解决不了/用户不满意）                           │
│  Level 2: 协商处理层（问清楚，给方案，用户确认）              │
│           ↓（用户拒绝方案/胡搅蛮缠）                         │
│  Level 3: 兜底流程层（收集信息，创建工单）                    │
│                                                              │
└─────────────────────────────────────────────────────────────┘
四、实施方案
方案选择：方案 B+（协商层 + Summary Agent）
方案	改动	优点	缺点	选择
方案 A：优化现有 LLM 提取	最小	改动小	不够灵活	❌
方案 B+：协商层 + Summary Agent	中等	职责清晰，灵活	需要新增节点	✅
方案 C：完全重新设计	最大	最灵活	风险高	❌
五、详细设计
5.1 意图识别分层
文件：config/intent_recognition_llm_cfg.json

修改内容：

{
    "sp": "# 角色定义\n你是意图识别专家。\n\n# 任务\n判断用户意图。\n\n# 意图类型\n\n## 不在兜底流程中时：\n- 问候：你好、您好、在吗\n- 使用指导：如何使用、怎么扫码、退款、扣钱、账单、发票（正常咨询）\n- 故障处理：充不进去、充不上、故障、坏了\n- **协商处理**：我要退款、多扣钱了、优惠券没用（没有强烈不满） ← 新增\n- 投诉兜底：垃圾、投诉、转人工、强烈不满、气死我了\n- 满意：谢谢、感谢、好的\n- 不满意：没用、不行、没帮助\n- 闲聊：天气、你是谁、无意义字符\n\n## 在兜底流程中时：\n- 继续兜底：用户提供信息、纠正问题、确认\n- 退出兜底：用户问使用问题、问故障问题、明确取消\n\n# 关键判断规则\n\n【协商处理 vs 投诉兜底】\n- \"我要退款\" → 协商处理（没有强烈不满）\n- \"我要退款，垃圾系统\" → 投诉兜底（有强烈不满）\n- \"多扣了我钱\" → 协商处理\n- \"多扣了我钱，什么破系统\" → 投诉兜底\n- \"转人工\" → 投诉兜底\n- \"能退款吗\" → 协商处理\n\n【投诉兜底的判断】（需谨慎）\n- 只有用户明确表达不满、要求投诉、转人工时才判断为投诉兜底\n- 包含情绪词：垃圾、太差、气死、举报、投诉\n- 正常咨询退款、扣钱、账单 → 协商处理\n\n【协商处理】的信号：\n1. 用户说\"我要退款\"但没有情绪词\n2. 用户说\"多扣钱了\"但没有投诉\n3. 用户说\"优惠券没用上\"但没有不满\n\n【直接进入兜底】的信号：\n1. 用户说\"垃圾\"、\"太差\"、\"气死我了\"\n2. 用户说\"我要投诉\"、\"转人工\"\n3. 用户强烈不满（连续重复、感叹号多）\n",
    "up": "{{fallback_context}}\n\n用户消息：{{user_message}}\n\n意图："
}
json
5.2 新增协商处理节点
文件：src/graphs/nodes/negotiate_node.py（新建）

"""
协商处理节点 - 问清楚情况，给方案，用户确认

核心原则：
1. 先追问详细情况（给用户诉说欲）
2. 根据知识库给初步解决方案
3. 问用户是否接受
4. 用户接受 → 问题解决；用户拒绝 → 进入兜底
"""
import os
import json
import logging
import re

from langchain_core.runnables import RunnableConfig
from langgraph.runtime import Runtime
from coze_coding_utils.runtime_ctx.context import Context
from tools.llm import create_llm_client
from langchain_core.messages import HumanMessage

from graphs.state import NegotiateInput, NegotiateOutput

logger = logging.getLogger(__name__)


def negotiate_node(
    state: NegotiateInput,
    config: RunnableConfig,
    runtime: Runtime[Context]
) -> NegotiateOutput:
    """
    title: 协商处理 - 问清楚情况，给方案
    desc: 先追问详细情况，根据知识库给方案，用户接受就解决，不接受再兜底
    integrations: 大语言模型
    """
    ctx = runtime.context
    user_message = state.user_message.strip()
    conversation_history = state.conversation_history or []
    
    logger.info(f"协商处理 - 用户消息：{user_message}")
    
    # 构建上下文（最近 3 轮对话）
    recent_history = ""
    if conversation_history:
        for i, msg in enumerate(conversation_history[-6:], 1):  # 最近 3 轮（用户+AI 是 2 条）
            role = "用户" if i % 2 == 1 else "AI"
            recent_history += f"{role}: {msg.get('content', '')}\n"
    
    # 调用 LLM 生成追问和方案
    prompt = f"""你是一个专业的客服助手。用户有退款/扣费/优惠券等问题，但没有强烈不满。

【任务】
1. 追问详细情况（给用户诉说欲，让用户把事情说清楚）
2. 根据常识给初步解决方案
3. 问用户是否接受这个方案

【对话历史】
{recent_history if recent_history else "无历史对话"}

【当前用户消息】
{user_message}

【输出格式】
请返回 JSON 格式：
{{
    "understanding": "你理解的用户问题（详细，包含原因）",
    "question": "追问的问题（如'请问您使用的哪个平台的优惠券？'）",
    "solution": "初步解决方案（如'建议先在 App 里查看优惠券使用记录'）",
    "ask_accept": "是否询问接受（如'您看这样可以吗？'）"
}}

如果信息已经完整，可以不追问，直接给方案。

请直接返回 JSON 格式，不要其他说明："""

    try:
        client = create_llm_client(ctx=ctx, provider="doubao")
        response = client.invoke(
            messages=[HumanMessage(content=prompt)],
            model="doubao-seed-1-8-251228",
            temperature=0.3,
            max_completion_tokens=300
        )
        
        # 解析 LLM 返回
        content = str(response.content).strip()
        if content.startswith("```"):
            content = re.sub(r'^```json?\s*', '', content)
            content = re.sub(r'\s*```$', '', content)
        
        result = json.loads(content)
        
        understanding = result.get("understanding", "")
        question = result.get("question", "")
        solution = result.get("solution", "")
        ask_accept = result.get("ask_accept", "")
        
        # 生成回复
        reply_parts = []
        
        # 1. 复述理解（让用户感觉被理解）
        if understanding:
            reply_parts.append(f"明白了，{understanding}。")
        
        # 2. 追问问题
        if question:
            reply_parts.append(question)
        
        # 3. 给方案
        if solution:
            reply_parts.append(f"建议：{solution}")
        
        # 4. 问是否接受
        if ask_accept:
            reply_parts.append(ask_accept)
        
        reply_content = "\n".join(reply_parts) if reply_parts else "好的，请问您具体遇到了什么情况？"
        
        logger.info(f"协商处理 - 回复：{reply_content}")
        
        return NegotiateOutput(
            reply_content=reply_content,
            negotiate_phase="asking",  # 追问阶段
            problem_understanding=understanding
        )
        
    except Exception as e:
        logger.error(f"协商处理失败：{e}")
        return NegotiateOutput(
            reply_content="好的，请问您具体遇到了什么情况？能详细说说吗？",
            negotiate_phase="asking",
            problem_understanding=""
        )
python
5.3 修改路由逻辑
文件：src/graphs/graph.py

修改内容：

# 新增导入
from graphs.nodes.negotiate_node import negotiate_node

# 添加协商处理节点
builder.add_node(
    "negotiate",
    negotiate_node,
    metadata={
        "type": "agent",
        "llm_cfg": "config/negotiate_llm_cfg.json"  # 可选，如果用上面的 prompt 可以不需要
    }
)

# 修改意图路由
builder.add_conditional_edges(
    source="intent_recognition",
    path=cond_intent_recognition_path,
    path_map={
        "使用指导": "query_rewrite",
        "故障处理": "query_rewrite",
        "协商处理": "negotiate",  # ← 新增
        "兜底流程": "fallback",
        "不满意": "dissatisfied",
        "满意": "satisfied",
        "评价反馈": "feedback",
        "退出兜底": "clear_fallback_state"
    }
)

# 协商处理后路由（需要新增条件节点）
builder.add_edge("negotiate", "save_history")

# 在 cond_fallback_node.py 中添加协商判断
# 如果用户拒绝协商方案 → 进入兜底
# 如果用户接受 → 结束
python
5.4 新增协商后条件判断
文件：src/graphs/nodes/cond_negotiate_node.py（新建）

"""
协商处理条件节点 - 判断用户是否接受方案
"""
from langgraph.runtime import Runtime
from graphs.state import NegotiateInput


def cond_negotiate_route_path(state: NegotiateInput) -> str:
    """
    协商后路由判断：
    - 用户接受方案 → 结束
    - 用户拒绝方案 → 进入兜底
    - 用户继续追问 → 继续协商
    """
    user_message = state.user_message.lower()
    
    # 接受信号
    accept_keywords = ["好的", "可以", "行", "试试", "谢谢", "没问题"]
    for kw in accept_keywords:
        if kw in user_message:
            return "end"
    
    # 拒绝信号
    reject_keywords = ["不行", "不可以", "不接受", "必须", "一定要", "马上"]
    for kw in reject_keywords:
        if kw in user_message:
            return "fallback"
    
    # 默认继续协商
    return "negotiate"
python
5.5 优化 Summary Agent（方案 B）
文件：src/graphs/nodes/summary_agent_node.py（新建）

"""
Summary Agent - 生成详细的问题总结

核心原则：
1. 总结要详细，包含原因（如"因为优惠券没使用，所以多扣了钱要求退款"）
2. 总结后让用户确认
3. 用户纠正就更新
"""
import os
import json
import logging
import re

from langchain_core.runnables import RunnableConfig
from langgraph.runtime import Runtime
from coze_coding_utils.runtime_ctx.context import Context
from tools.llm import create_llm_client
from langchain_core.messages import HumanMessage

from graphs.state import SummaryInput, SummaryOutput

logger = logging.getLogger(__name__)


def summary_agent_node(
    state: SummaryInput,
    config: RunnableConfig,
    runtime: Runtime[Context]
) -> SummaryOutput:
    """
    title: Summary Agent - 生成详细问题总结
    desc: 根据对话历史生成详细的问题总结，包含原因和诉求
    integrations: 大语言模型
    """
    ctx = runtime.context
    conversation_history = state.conversation_history or []
    
    logger.info(f"Summary Agent - 生成问题总结")
    
    # 构建对话历史
    history_text = ""
    if conversation_history:
        for i, msg in enumerate(conversation_history[-10:], 1):  # 最近 5 轮
            role = "用户" if i % 2 == 1 else "AI"
            history_text += f"{role}: {msg.get('content', '')}\n"
    
    # 调用 LLM 生成详细总结
    prompt = f"""你是一个专业的客服助手。请根据对话历史生成详细的问题总结。

【对话历史】
{history_text}

【任务】
生成详细的问题总结，包含：
1. 用户遇到了什么问题
2. 问题的原因是什么（如果有提到）
3. 用户的诉求是什么（退款、赔偿、维修等）

【输出格式】
请返回 JSON 格式：
{{
    "detailed_summary": "详细总结（包含原因，如'因为优惠券未成功使用，导致多扣了 10 元钱，用户要求退款'）",
    "simple_problem": "简单问题（如'退款'）"
}}

请直接返回 JSON 格式，不要其他说明："""

    try:
        client = create_llm_client(ctx=ctx, provider="doubao")
        response = client.invoke(
            messages=[HumanMessage(content=prompt)],
            model="doubao-seed-1-8-251228",
            temperature=0.2,
            max_completion_tokens=200
        )
        
        # 解析 LLM 返回
        content = str(response.content).strip()
        if content.startswith("```"):
            content = re.sub(r'^```json?\s*', '', content)
            content = re.sub(r'\s*```$', '', content)
        
        result = json.loads(content)
        
        detailed_summary = result.get("detailed_summary", "")
        simple_problem = result.get("simple_problem", "")
        
        logger.info(f"Summary Agent - 详细总结：{detailed_summary}")
        
        return SummaryOutput(
            detailed_summary=detailed_summary,
            simple_problem=simple_problem
        )
        
    except Exception as e:
        logger.error(f"Summary Agent 失败：{e}")
        return SummaryOutput(
            detailed_summary="",
            simple_problem=""
        )
python
六、测试验证
测试场景（10 个）
TEST_SCENARIOS = [
    # 标准场景（应该通过协商层解决）
    {"id": "S001", "name": "退款咨询", 
     "initial_message": "我要退款",
     "expected_flow": "negotiate"},  # 预期进入协商层
    
    {"id": "S002", "name": "优惠券问题", 
     "initial_message": "优惠券没用上，能退吗",
     "expected_flow": "negotiate"},
    
    {"id": "S003", "name": "多扣费咨询", 
     "initial_message": "多扣了我钱",
     "expected_flow": "negotiate"},
    
    # 应该直接进入兜底的场景
    {"id": "S004", "name": "情绪投诉", 
     "initial_message": "垃圾系统，气死我了",
     "expected_flow": "fallback"},  # 预期进入兜底
    
    {"id": "S005", "name": "明确投诉", 
     "initial_message": "我要投诉",
     "expected_flow": "fallback"},
    
    {"id": "S006", "name": "要求人工", 
     "initial_message": "转人工客服",
     "expected_flow": "fallback"},
    
    # 协商不成进入兜底的场景
    {"id": "S007", "name": "拒绝方案", 
     "initial_message": "我要退款",
     "step2_reply": "不行，我必须现在退款",
     "expected_flow": "negotiate → fallback"},
    
    # 标准兜底流程场景
    {"id": "S008", "name": "用户纠正", 
     "initial_message": "我要投诉，多扣了我钱",
     "correction_message": "不对，是优惠券没用上",
     "expected_flow": "fallback"},
    
    {"id": "S009", "name": "语音格式", 
     "initial_message": "我要投诉",
     "phone_format": "139。16425678",
     "expected_flow": "fallback"},
    
    {"id": "S010", "name": "多次补充", 
     "initial_message": "我要投诉，多扣钱",
     "step3_message": "还有优惠券没用",
     "expected_flow": "fallback"}
]
python
预期效果
场景分类	修复前	修复后
协商处理场景（S001-003）	❌ 直接兜底	✅ 协商层解决
投诉兜底场景（S004-006）	✅ 直接兜底	✅ 直接兜底
拒绝方案场景（S007）	❌ 直接兜底	✅ 协商 → 兜底
标准兜底场景（S008-010）	✅ 兜底	✅ 兜底
预期通过率：

协商处理场景：3/3 = 100%
投诉兜底场景：3/3 = 100%
拒绝方案场景：1/1 = 100%
总体通过率：7/10 = 70%（协商场景不创建工单，也算成功）
七、实施步骤
第 1 步：修改意图识别（1 小时）
1. 修改 config/intent_recognition_llm_cfg.json
2. 新增"协商处理"意图类型
3. 添加判断规则（协商处理 vs 投诉兜底）
bash
第 2 步：新增协商节点（2-3 小时）
1. 创建 src/graphs/nodes/negotiate_node.py
2. 创建 src/graphs/nodes/cond_negotiate_node.py
3. 添加 state.py 中的 NegotiateInput/Output
bash
第 3 步：修改路由逻辑（1 小时）
1. 修改 src/graphs/graph.py
2. 添加 negotiate 节点
3. 添加协商后路由
bash
第 4 步：新增 Summary Agent（2-3 小时）
1. 创建 src/graphs/nodes/summary_agent_node.py
2. 在兜底流程中调用 Summary Agent
3. 优化问题总结格式
bash
第 5 步：测试验证（1 小时）
1. 执行 10 个场景测试
2. 验证协商层是否正确触发
3. 验证兜底是否不再"无条件"
bash
第 6 步：部署上线（30 分钟）
1. 代码 review
2. 部署到生产环境
3. 监控线上表现
bash
八、验收标准
功能验收
验收项	通过标准
协商层触发	"我要退款"等场景进入协商层，不直接兜底
投诉兜底	"垃圾系统"等场景直接兜底，不经过协商
拒绝方案	用户拒绝协商方案后进入兜底
总结详细	问题总结包含原因（如"因为优惠券..."）
确认步骤	总结后用户确认再进入信息收集
数据验收
-- 协商场景不应该创建工单
SELECT COUNT(*) FROM case_records 
WHERE problem_summary LIKE '%退款%' 
AND problem_summary NOT LIKE '%垃圾%'
AND problem_summary NOT LIKE '%投诉%';
-- 预期：数量减少

-- 兜底场景应该创建工单
SELECT COUNT(*) FROM case_records 
WHERE problem_summary LIKE '%投诉%' 
OR problem_summary LIKE '%垃圾%';
-- 预期：数量稳定
sql
九、风险与应对
风险 1：协商层无法解决问题
表现：用户反复追问，协商层无法收敛

应对：

设置最大协商轮数（如 3 轮）
超过轮数自动进入兜底
风险 2：协商层误判为兜底
表现：用户说"我要退款"，协商层还是进入兜底

应对：

优化意图识别 Prompt
增加测试场景覆盖
风险 3：Summary Agent 总结不准确
表现：总结遗漏关键信息

应对：

优化 Summary Agent Prompt
增加用户确认步骤
十、变更历史
版本	日期	变更内容	状态
v1.0	2026-04-09	初始方案	已完成
v1.1	2026-04-09	P1/P2 修复	已完成 ✅
v2.0	2026-04-09	分层客服架构	待实现
十一、下一步
✅ P1/P2 修复 — 已完成并验证
⏳ 分层客服实现 — 按步骤 1-6 实现协商层
⏳ 测试验证 — 执行 10 个场景测试，目标通过率 ≥ 70%
⏳ 部署上线 — 手动 review 后部署到生产
版本：v2.0 | 最后更新：2026-04-09